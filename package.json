{
    "name": "dash",
    "version": "1.0.0",
    "description": "Dash cloud server & daemon client",
    "author": "Anuv Gupta",
    "license": "ISC",
    "scripts": {
        "postinstall": "(cd src/daemon && npm install); (cd src/cloud/backend && npm install); (cd src/cloud/frontend && npm install)",
        "clean": "rm -rf dist",
        "prebuild": "npm run clean && mkdir -p dist",
        "build": "(cd src/daemon && npm run build); (cd src/cloud/backend && npm run build); (cd src/cloud/frontend && npm run build)"
    }
}
