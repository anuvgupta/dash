{
    "name": "dash",
    "version": "1.0.0",
    "description": "Dash cloud server & daemon client",
    "author": "Anuv Gupta",
    "license": "ISC",
    "scripts": {
        "postinstall": "(cd src/app/cloud && npm install); (cd src/app/daemon && npm install)",
        "clean": "rm -rf dist",
        "prebuild": "npm run clean && mkdir -p dist",
        "build": "(cd src/app/cloud && npm run build); (cd src/app/daemon && npm run build)"
    }
}
